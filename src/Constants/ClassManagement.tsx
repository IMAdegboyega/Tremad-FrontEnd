// ClassManagement constants
//
// Previously exported a mock `subjects` array consumed by the student
// SubjectManagement section. The backend doesn't expose a subjects-per-
// student endpoint yet, so the UI renders an empty state directly inside
// the component. File kept as an empty module for path stability.
export {};
