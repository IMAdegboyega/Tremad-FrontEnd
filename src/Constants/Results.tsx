// Results constants
//
// Mock data was previously exported from this file (stats + subjectResults)
// and consumed by the Results section. All of that is now derived from the
// real /student/academic/results API call inside the Results section and
// its child components. This file intentionally exports nothing today; it
// is kept so the path continues to resolve for any lingering import.
export {};
