// Timetable constants
//
// Mock data (stats, timeSlots, TimeSchedule) used to live here. The views now
// derive these from real /student/academic/timetable data inside the TimeTable
// section + its child components. Kept as an empty module for path stability.
export {};
